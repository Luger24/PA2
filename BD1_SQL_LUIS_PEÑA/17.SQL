SELECT COUNTRY_NAME, CITY, COUNT(DEPARTMENT_ID) DEPARTAMENTOS_QUE_SE_EJECUTAN
FROM HR.COUNTRIES 
JOIN HR.LOCATIONS USING (COUNTRY_ID) 
JOIN HR.DEPARTMENTS USING (LOCATION_ID) 
GROUP BY COUNTRY_NAME, CITY;